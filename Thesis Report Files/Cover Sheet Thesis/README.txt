Uncomment and run the first code chunk if you don't have any of the libraries installed, as they aren't present in base R and hence require a special installation. Itsadug package is not available in base R, if you don't have this package, install it from github. 
The code for installation is commented below. Please run it before knitting.



load the package devtools:

install the newest version from itsadug:

devtools::install_github("vr-vr/itsadug", build_vignettes=TRUE)